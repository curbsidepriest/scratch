import { PiecesCollection } from "@/components/PiecesCollection";

// The collection of pieces — a home for finished and in-progress work (§2).
export default function PiecesPage() {
  return <PiecesCollection />;
}

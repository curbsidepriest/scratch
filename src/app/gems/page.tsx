import { GemsCollection } from "@/components/GemsCollection";

// The gem library — every snippet extracted from every session, flat and dated,
// separate from the raw sessions on the home Scratchpad (spec §3).
export default function GemsPage() {
  return <GemsCollection />;
}

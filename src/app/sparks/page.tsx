import { SparksCollection } from "@/components/SparksCollection";

// The Sparks library — threads set aside with "Not now", kept to develop later.
export default function SparksPage() {
  return <SparksCollection />;
}
